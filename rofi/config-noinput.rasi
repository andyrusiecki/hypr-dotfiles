@import "./config.rasi"

inputbar {
  enabled: false;
}

prompt {
  enabled: false;
}

message {
  padding: 8px 12px;
  border: none;
}

message textbox {
  padding: 0;
  font: inherit;
}

mainbox {
  children: [ message, listview ];
}
